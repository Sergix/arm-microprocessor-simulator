pub struct Options {
    pub(crate) memory_size: usize,
    pub(crate) elf_file: String
}

impl Options {
    pub fn Parse(&self) {
        // parse the command line
        // validates the options
        // stores the results
    }
}